namespace bindings.c.libc;

// C Standard says:
// `1 == sizeof(char) <= sizeof(short) <= sizeof(int) <= sizeof(long) <= sizeof(long long)`
// A `short` must be at least 16 bits long.
// An `int` must be at least 16 bits long.
// A `long` must be at least 32 bits long.
// A `long long` must be at least 64 bits long.

public struct c_int
{
    // TODO define standard math operators
}
